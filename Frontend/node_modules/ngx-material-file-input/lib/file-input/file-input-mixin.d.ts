import { FormGroupDirective, NgControl, NgForm } from '@angular/forms';
import { CanUpdateErrorStateCtor, ErrorStateMatcher } from '@angular/material/core';
/** @docs-private */
export declare class FileInputBase {
    _defaultErrorStateMatcher: ErrorStateMatcher;
    _parentForm: NgForm;
    _parentFormGroup: FormGroupDirective;
    ngControl: NgControl;
    constructor(_defaultErrorStateMatcher: ErrorStateMatcher, _parentForm: NgForm, _parentFormGroup: FormGroupDirective, ngControl: NgControl);
}
/**
 * Allows to use a custom ErrorStateMatcher with the file-input component
 */
export declare const FileInputMixinBase: CanUpdateErrorStateCtor & typeof FileInputBase;
